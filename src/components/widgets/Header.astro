---
import Logo from '~/components/Logo.astro';

import type { CallToAction } from '~/types';

export interface Props {
  id?: string;
  links?: Array<{
    text?: string;
    href?: string;
    ariaLabel?: string;
  }>;
  actions?: Array<CallToAction>;
  isSticky?: boolean;
  isDark?: boolean;
  isFullWidth?: boolean;
  showToggleTheme?: boolean;
  showRssFeed?: boolean;
  position?: string;
}

const {
  id = 'header',
  links = [],
  actions = [],
  isSticky = false,
} = Astro.props;
---

<header
  id={id}
  class="header-main"
>
  <div class="header-container">
    <div class="header-content">
      <!-- Logo -->
      <div class="logo-wrapper">
        <a href="/" class="logo-link">
          <Logo />
        </a>
      </div>

      <!-- Desktop Navigation -->
      <nav class="desktop-nav">
        {links.map(({ text, href, ariaLabel }) => (
          <a
            href={href}
            aria-label={ariaLabel || text}
            class="nav-link"
          >
            {text}
          </a>
        ))}
      </nav>

      <!-- CTA Button -->
      {actions?.length ? (
        <div class="cta-wrapper">
          {actions.map((action) => (
            <a
              href={action.href}
              target={action.target}
              class="cta-button"
            >
              {action.text}
            </a>
          ))}
        </div>
      ) : null}

      <!-- Mobile Menu Button -->
      <button
        type="button"
        class="mobile-menu-btn"
        aria-label="Toggle menu"
      >
        <svg class="menu-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>
</header>

<style>
  .header-main {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 9999;
    width: 100%;
    background: rgba(15, 23, 42, 0.4) !important;
    backdrop-filter: blur(16px) saturate(180%);
    -webkit-backdrop-filter: blur(16px) saturate(180%);
    border: none !important;
    border-bottom: none !important;
    box-shadow: none !important;
  }

  .header-main::after {
    content: none !important;
  }

  .header-container {
    width: 100%;
    max-width: 100%;
    margin: 0;
    padding: 1rem 2rem;
    background: transparent !important;
    border: none !important;
  }

  .header-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: transparent !important;
    width: 100%;
    gap: 2rem;
    border: none !important;
  }

  .logo-wrapper {
    display: flex;
    align-items: center;
    background: transparent !important;
    flex-shrink: 0;
    margin-right: auto;
  }

  .logo-link {
    display: flex;
    align-items: center;
    transition: opacity 0.2s;
    background: transparent !important;
  }

  .logo-link:hover {
    opacity: 0.8;
  }

  .desktop-nav {
    display: none;
    align-items: center;
    gap: 3rem;
    background: transparent !important;
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
  }

  @media (min-width: 768px) {
    .desktop-nav {
      display: flex;
    }
  }

  .nav-link {
    color: rgba(255, 255, 255, 0.95) !important;
    font-weight: 500;
    font-size: 0.9375rem;
    transition: color 0.2s;
    text-decoration: none;
    background: transparent !important;
    white-space: nowrap;
  }

  .nav-link:hover {
    color: #67e8f9 !important;
  }

  .cta-wrapper {
    display: none;
    align-items: center;
    background: transparent !important;
    flex-shrink: 0;
    margin-left: auto;
  }

  @media (min-width: 768px) {
    .cta-wrapper {
      display: flex;
    }
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.625rem 1.5rem;
    font-size: 0.875rem;
    font-weight: 600;
    color: #ffffff !important;
    background: linear-gradient(135deg, #BD5931 0%, #9A4826 100%) !important;
    border-radius: 0.5rem;
    transition: all 0.3s ease;
    text-decoration: none;
    box-shadow: 0 4px 12px rgba(189, 89, 49, 0.2);
    border: none !important;
  }

  .cta-button:hover {
    background: linear-gradient(135deg, #9A4826 0%, #7d3a1f 100%) !important;
    box-shadow: 0 8px 20px rgba(189, 89, 49, 0.35);
    transform: translateY(-2px);
  }

  .mobile-menu-btn {
    display: flex;
    align-items: center;
    color: #ffffff !important;
    background: transparent !important;
    border: none;
    cursor: pointer;
    transition: color 0.2s;
    padding: 0;
    flex-shrink: 0;
  }

  @media (min-width: 768px) {
    .mobile-menu-btn {
      display: none;
    }
  }

  .mobile-menu-btn:hover {
    color: #67e8f9 !important;
  }

  .menu-icon {
    width: 1.5rem;
    height: 1.5rem;
  }

  /* Force remove any borders globally */
  header *,
  header *::before,
  header *::after {
    border-bottom: none !important;
    box-shadow: none !important;
  }
</style>
